{
  "name": "web-e2e",
  "version": "0.1.0",
  "description": "End to end tests for web",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf dist",
    "playwright": "./node_modules/.bin/playwright",
    "e2e": "playwright test",
    "e2e:report": "playwright show-report"
  },
  "keywords": [
    "web",
    "e2e"
  ],
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.35.1"
  },
  "dependencies": {
    "dotenv": "^16.1.4"
  }
}
